class player(dimension, name, spr_adr) {
  var x, y, z, name, sprite_address;
  x = dimension.x;
  y = dimension.y;
  z = dimension.z;
  name = name;
  sprite_address = spr_adr;
}

class dimension(x, y, z) {
  var x, y, z;
  x = x;
  y = y;
  z = z;
}
